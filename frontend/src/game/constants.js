// Constantes de reglas del juego que el frontend necesita conocer para la UI
// (validaciones optimistas antes de llamar al backend, que es quien manda).
// Deben mantenerse en sync con backend RoomService / spec.md.

export const MIN_PLAYERS_TO_START = 3;
